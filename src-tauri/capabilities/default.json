{
  "$schema": "https://schema.tauri.app/permission/2",
  "identifier": "default",
  "description": "Default capabilities for the settings window",
  "windows": ["settings"],
  "permissions": ["core:default"]
}
