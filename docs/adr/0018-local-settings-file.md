# Local Settings File

Slugtale will store v1 preferences in a local non-secret settings file rather than the OS keychain or credential store. The first version has no API keys, account tokens, or other secrets, so a simple local config file is easier to inspect, back up, and debug.
