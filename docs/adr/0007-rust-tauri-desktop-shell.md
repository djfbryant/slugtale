# Rust and Tauri Desktop Shell

Slugtale will use Rust with Tauri for the first desktop application shell. This keeps the core services in Rust while giving the app a cross-platform resident desktop surface for setup, tray/menu-bar status, permissions guidance, model management, and later updater support, instead of building separate native shells for macOS and Windows.
