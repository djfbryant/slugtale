# Tray/Menu-Bar Resident First

Slugtale's first version will behave primarily as a tray/menu-bar resident app with a small settings window. A broader normal application UI is deferred because the first workflow is dictating into other apps, so always-available background presence matters more than an expansive in-app workspace.
