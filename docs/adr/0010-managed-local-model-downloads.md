# Managed Local Model Downloads

Slugtale will manage Whisper model download during onboarding and store the model file locally, with a visible model path and a way to remove the model later. Network access for model download does not weaken the local-only processing boundary: captured speech and transcriptions still stay on the user's machine.
