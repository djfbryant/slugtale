# macOS First with Platform Adapters

Slugtale's first implementation slice will target macOS first while shaping platform-specific behavior behind adapters for later Windows support. This keeps the first vertical slice small enough to validate end to end, without baking macOS-only assumptions into the core dictation workflow.
