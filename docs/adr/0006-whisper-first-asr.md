# Whisper-First ASR

Slugtale's first version will use Whisper through an embeddable local runtime, with model files stored on the user's machine. This favors a proven macOS and Windows path over Parakeet's lower-latency promise, while keeping the speech recognition boundary replaceable so Parakeet or another local engine can be evaluated later.
