# Clipboard Rescue on Insertion Failure

Slugtale normally avoids placing transcriptions on the clipboard, but if text insertion fails after dictation completes, it will copy the transcription to the clipboard and notify the user. This is a deliberate exception to prevent data loss without introducing persistent dictation history.
