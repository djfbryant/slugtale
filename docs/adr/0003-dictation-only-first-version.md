# Dictation-Only First Version

Slugtale's first version will support dictation only: the user activates a hotkey, speaks, and Slugtale inserts the resulting transcription into the current text target. Rewrite workflows for selected text are deliberately deferred so the first version can harden permissions, audio capture, transcription, and text insertion before adding selection reading and text transformation.
