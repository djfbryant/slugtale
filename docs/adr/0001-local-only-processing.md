# Local-Only Processing

Slugtale will treat local-only processing as a product boundary: captured speech, transcriptions, and optional enhancement must run on the user's machine and must not depend on cloud APIs. This rejects easier cloud speech-to-text and cloud cleanup paths in exchange for privacy, offline operation, and predictable control over user data.
