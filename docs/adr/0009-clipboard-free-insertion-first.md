# Clipboard-Free Insertion First

Slugtale will try clipboard-free text insertion before using clipboard-based paste fallbacks. This preserves the no-history privacy posture by avoiding unnecessary exposure to clipboard managers, while still allowing a compatibility fallback when direct insertion is unavailable or unreliable in a target application.
