# Lightweight Deterministic Cleanup

Slugtale will apply lightweight deterministic cleanup to final transcriptions before insertion, such as trimming whitespace, normalizing repeated spaces, and capitalizing the first character when appropriate. LLM-style rewriting or semantic enhancement is deferred so v1 stays local, predictable, and easy to explain.
