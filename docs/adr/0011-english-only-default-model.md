# English-Only Default Model

Slugtale's first version will default to an English-only Whisper model such as `base.en` or `small.en`. Multilingual dictation is deferred because the initial product is for English dictation, and the English-only models keep the first experience faster, smaller, and easier to evaluate.
